{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "chmod +x ./node_modules/.bin/json-server",
    "start": "npx json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
